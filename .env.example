# Copy this file to .env and fill in your own Postgres connection string.
# Format: postgresql://<user>:<password>@<host>:<port>/<database>

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/tanish_book

# Optional — used only when running locally with `python app.py`
FLASK_DEBUG=1
PORT=5000
